// Generate a random number between 1 and 10
const randomNumber = Math.floor(Math.random() * 10) + 1;

// Use the random number to create a new string with a length equal to the number
const randomString = 'a'.repeat(randomNumber);

// Return the new string
return randomString;
